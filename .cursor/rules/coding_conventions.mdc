---
description: Convencoes de codificacao - nomenclatura, arquivos e padroes
globs: ["lib/**/*.dart"]
alwaysApply: false
---

# Convencoes de Codificacao

## Nomenclatura

### Classes e tipos

- Entities: substantivo singular em PascalCase
- Value Objects: substantivo descritivo em PascalCase
- Use Cases: verbo no infinitivo em PascalCase
- Repositories abstratos: prefixo `I` + nome singular
- Repositories concretos: nome singular + `Repository`
- Services: nome singular + `Service`
- DTOs: nome singular + `DTO`
- Models: nome singular + `Model`
- Controllers: nome + `Controller`
- Providers: nome + `Provider`
- Failures e Exceptions: nome descritivo + `Failure` ou `Exception`

### Variaveis e metodos

- casing, verbos em metodos e prefixo `_` em membros privados seguem
  `coding_style.mdc`

### Arquivos

- o nome do arquivo deve seguir a classe principal
- interfaces de repositorio seguem o padrao `i_nome_repository.dart`
- casing de arquivos e diretorios segue `coding_style.mdc`

## Estrutura esperada por tipo

### Use cases

- uma unica responsabilidade
- dependencia via construtor
- metodo publico `call()`
- retorno com `Result<T>`

### Repositories

- contratos ficam no `domain`
- implementacoes ficam em `infrastructure`
- APIs publicas devem expor entidades, value objects ou `Result<T>`

### Services e mappers

- servicos de `application` orquestram fluxos
- mappers convertem entre camadas sem carregar logica de dominio

## Tratamento de erros

- operacoes que podem falhar retornam `Result<T>`, nao excecoes como
  fluxo normal de negocio
- modelo de erros de dominio: veja `domain_layer.mdc`
- mecanica de `result_dart` (`Success`/`Failure`): veja
  `dependencies_patterns.mdc`

## Injecao de dependencias

- injete dependencias via construtor
- nao acople classes diretamente ao `GetIt`
- a resolucao pelo container deve acontecer na borda de composicao
- o principio (DIP, depender de abstracoes) vive em
  `solid_principles.mdc`

## Fora do escopo desta regra

- comentarios e documentacao: veja `general_rules.mdc`
- casing, imports e estilo Dart detalhado: veja `coding_style.mdc`
- null safety: veja `null_safety.mdc`
- bibliotecas obrigatorias: veja `dependencies_patterns.mdc`
- principios SOLID: veja `solid_principles.mdc`
